<nav class="flex gap-4 bg-[#F3EEC3] p-4">
  <% for (let i = 0; i < firstLetters.length; i++) { %>
    <a href="<%= `/villagers/names/${firstLetters[i].firstletter}` %>">
      <span> <%= capitalize(firstLetters[i].firstletter) %> </span>
    </a>
  <% } %>
</nav>