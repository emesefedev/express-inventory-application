<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/style.css">
  </head>

  <body>
    <h1><%= capitalize(title) %></h1>
    <ul>
      <% villagersNames.map(villager => { %>
        <li> <a href=<%= `villagers/${villager.name}`%> >  <%= capitalize(villager.name) %>  </a> </li>
      <% }) %>
    </ul>
  </body>
</html>